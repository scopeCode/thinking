/**
 * Created by WG on 2015/11/2.
 */
var Sequelize = require('sequelize');

var mysql = {};

var config ={
    database:'baby',
    username:'root',
    password:'root',
    options:{
        host:'127.0.0.1',
        port:'3306',
        dialect : 'mysql',
        pool: {
            max: 5,
            min: 0,
            idle: 10000
        }
    }
};
exports.sequelize = new Sequelize(config.database, config.username, config.password, config.options);
exports.Sequelize = Sequelize;