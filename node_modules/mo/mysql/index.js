/**
 * Created by WG on 2015/11/2.
 */
var Sequelize = require('sequelize');

var mysql = {};

var config ={
    database:'',
    username:'root',
    password:'root',
    options:{
        host:'127.0.0.1',
        port:'3306',
        dialect : 'mysql'
    }
};

mysql.sequelize =  new Sequelize(config.database, config.username, config.password, config.options);

module.exports = mysql;